<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.condorlabs.skills_test">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name=".Base.App.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Implementation.Splash.Controller.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Implementation.Movie.Controller.MovieListActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".Implementation.Movie.Controller.MovieDetailActivity"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>